entryPoints:
  web:
    address: ":80"
  websecure:
    address: ":443"

providers:
  docker:
    exposedByDefault: false

api:
  dashboard: true

tls:
  certificates:
    - certFile: /certs/pristine-aiops.crt
      keyFile: /certs/pristine-aiops.key